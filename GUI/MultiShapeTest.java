package gui;

/**
 * This class serves as the entry point for launching the GUI application.
 * It contains the main method that initiates the launch of the GUI.
 */
public class MultiShapeTest {

    /**
     * The main method that launches the GUI application.
     *
     * @param args Command-line arguments (not used in this application).
     */
    public static void main(String[] args) {
        GUI.launch(GUI.class);
    }
}